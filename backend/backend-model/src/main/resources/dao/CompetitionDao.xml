<JdbcEntitySetup
        xmlns="http://www.ontimize.com/schema/jdbc"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.ontimize.com/schema/jdbc http://www.ontimize.com/schema/jdbc/ontimize-jdbc-dao.xsd"
        catalog="" schema="${mainschema}" table="COMPETITIONS"
        datasource="mainDataSource" sqlhandler="dbSQLStatementHandler">
    <DeleteKeys>
        <Column>COMP_ID</Column>
    </DeleteKeys>
    <UpdateKeys>
        <Column>COMP_ID</Column>
    </UpdateKeys>
    <GeneratedKey>comp_id</GeneratedKey>
    <Queries>
        <Query id="publicCount">
            <Sentence>
                <![CDATA[
                    WITH PUBLIC_COUNT AS (
                        SELECT c.COMP_ID, c.COMP_NAME, c.COMP_DESCRIPTION, c.COMP_ISPUBLIC, count(uc.USER_) AS NUM_USERS, COUNT(UC.USER_) > 10 AS COMPLETE
                        FROM COMPETITIONS c
                        INNER JOIN USERS_COMPETITIONS uc
                        ON c.COMP_ID = uc.COMP_ID
                        WHERE c.COMP_ISPUBLIC = true
                        GROUP BY c.COMP_ID
                        HAVING COUNT(UC.USER_) < 10)
                    SELECT #COLUMNS#
                    FROM PUBLIC_COUNT
                    #WHERE#
                    #ORDER#;
                ]]>
            </Sentence>
        </Query>
        <Query id="competitionById">
            <Sentence>
                <![CDATA[
                    WITH COMP_BY_ID AS
                        (SELECT C.COMP_ID, C.COMP_NAME, UC.USER_, UC.UC_ID, UC.UC_AVAILABLE_MONEY, UC.UC_POINTS, c.COMP_DESCRIPTION, c.COMP_CODE
                        FROM COMPETITIONS C
                        LEFT JOIN USERS_COMPETITIONS UC
                        ON C.COMP_ID = UC.COMP_ID
                        ORDER BY UC.UC_POINTS DESC)
                    SELECT #COLUMNS#
                    FROM COMP_BY_ID
                    #WHERE#
                    #ORDER#;
                ]]>
            </Sentence>
        </Query>
        <Query id="availableMoneyFilter">
            <Sentence>
                <![CDATA[
                    WITH AVAIL_MONEY AS
                        (SELECT C.COMP_ID, C.COMP_NAME, UC.USER_, UC.UC_ID, UC.UC_AVAILABLE_MONEY, UC.UC_POINTS, c.COMP_DESCRIPTION, c.COMP_CODE
                        FROM COMPETITIONS C
                        LEFT JOIN USERS_COMPETITIONS UC
                        ON C.COMP_ID = UC.COMP_ID
                        ORDER BY UC.UC_POINTS DESC)
                    SELECT #COLUMNS#
                    FROM AVAIL_MONEY
                    #WHERE#
                    #ORDER#;
                ]]>
            </Sentence>
        </Query>
    </Queries>
</JdbcEntitySetup>